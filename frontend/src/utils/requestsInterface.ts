export interface BarberProfileRequest {
  shopName: string;
  phoneNumber: string;
  address: string;
  city: string;
  pinCode: string;
  servicesOffered: string;
  startingPrice: number;
}
